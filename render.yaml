services:
  - type: web
    name: student-manager-app
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn day15_project.wsgi:application
